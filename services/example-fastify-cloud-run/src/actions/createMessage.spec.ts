import { describe, expect, test } from "vitest";

// import { createMessage } from "./createMessage";

describe("createMessage", () => {
  test("example", () => {
    const testString = "test";

    expect(testString).toBeTypeOf("string");
  });
});
